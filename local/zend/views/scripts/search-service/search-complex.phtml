<? if (!empty($this->itemList)): ?>
    <? foreach ($this->itemList as $itemId => $item): ?>
        <div class="radio-cell">
            <input type="radio" name="complexId" id="complexId-<?= $itemId ?>" value="<?= $itemId ?>">
            <label for="complexId-<?= $itemId ?>" title="<?= $item ?>"><?= $item ?></label>
        </div>
    <? endforeach ?>
<? else: ?>
    <?= $this->partial("_partials/not-found-status/complex.phtml", ["message" => " <p>Подходящих вариантов жилых комплексов по вашему запросу не найдено.</p><p>Попробуйте ввести <b>другие ключевые слова.</b></p>"]) ?>
<? endif ?>
